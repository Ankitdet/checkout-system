{
  "name": "checkout-system",
  "version": "1.0.0",
  "description": "A checkout system with flexible pricing rules for a computer store.",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "rimraf dist",
    "start": "node dist/index.js",
    "build": "npm run prebuild && tsc",
    "dev": "ts-node-dev src/index.ts",
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "keywords": [
    "typescript"
  ],
  "author": "apdetroja",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.8.4",
    "jest": "^29.7.0",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.2.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.0"
  }
}
